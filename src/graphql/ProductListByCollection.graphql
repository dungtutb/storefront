query ProductListByCollection($slug: String!, $channel: String!, $first: Int!) {
	collection(slug: $slug, channel: $channel) {
		name
		description
		seoDescription
		seoTitle
		products(first: $first) {
			edges {
				node {
					...ProductListItem
				}
			}
		}
	}
}
